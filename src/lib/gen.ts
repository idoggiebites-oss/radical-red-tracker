/** Gen 9 in the vendored data set carries the Radical Red changes.
 * Its own module so the light-weight name lookups in `itemNames.ts` can
 * share it without importing the engine adapter. */
export const GEN = 9;
